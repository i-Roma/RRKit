import XCTest
@testable import RRKit

final class RRKitTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        //XCTAssertEqual(RRKit().text, "Copy-Paste")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
