//
//  Fonts.swift
//

import UIKit

public struct Font {
    
    struct primary {
        private static let primary = "Avenir"
        static let light = "\(primary)-Light"
        static let medium = "\(primary)-Medium"
        static let bold = "\(primary)-Bold"
        static let black = "\(primary)-Black"
        static let heavy = "\(primary)-Heavy"
        static let font = light
    }
    
}
